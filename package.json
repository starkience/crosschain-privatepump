{
  "name": "crosschain-private-launchpad",
  "version": "0.1.0",
  "private": true,
  "license": "Apache-2.0",
  "type": "module",
  "packageManager": "pnpm@10.34.5",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "pnpm --filter @private-launchpad/sdk build && pnpm --filter @private-launchpad/demo build && pnpm --filter @private-launchpad/relayer build && pnpm build:evm",
    "build:official-bridge": "node scripts/build-official-bridge.mjs",
    "build:evm": "pnpm setup:evm && forge build --root evm",
    "typecheck": "pnpm --filter @private-launchpad/sdk build && pnpm --filter @private-launchpad/demo typecheck && pnpm --filter @private-launchpad/relayer typecheck",
    "test": "pnpm test:sdk && pnpm test:demo && pnpm test:relayer && pnpm test:evm",
    "test:sdk": "pnpm --filter @private-launchpad/sdk test",
    "test:demo": "pnpm --filter @private-launchpad/demo test",
    "test:relayer": "pnpm --filter @private-launchpad/relayer test",
    "test:evm": "pnpm setup:evm && forge test --root evm",
    "setup:evm": "cd evm && forge soldeer install --config-location foundry",
    "verify:sepolia": "node scripts/verify-sepolia.mjs",
    "format:check": "prettier --check 'scripts/**/*.mjs' 'docs/**/*.md' 'config/**/*.json' '.github/workflows/*.yml' package.json pnpm-workspace.yaml README.md && pnpm --filter @private-launchpad/sdk format:check && pnpm --filter @private-launchpad/demo format:check && pnpm --filter @private-launchpad/relayer format:check && forge fmt --root evm --check"
  },
  "devDependencies": {
    "prettier": "^3.6.2"
  }
}
